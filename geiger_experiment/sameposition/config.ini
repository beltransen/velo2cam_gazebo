[general]
corner_dist=0.125 # 4 cm corner distance

[calibration]
a1_active=0 # fix alpha to 0 during optimization
k5_active=0 # fix k5 to 0 during optimization (no wide angle lens)

[range]
fine_max_scale_correction=0.0
fine_outlier_multiplier=1.5
fine_max_outlier_ratio=0.05
fine_max_number_of_results=3
fine_non_max_suppression=1.5
